

        let columnData = contractData[0];
  columnData = columnData.map(
    (c, index) =>
      c[0] !== "DONOTSHOW" && {
        label: c[0],
        field: c[0],
        sort: "asc"
      },
    {
      label: "View Trips",
      field: "viewTrips"
    }
  );
